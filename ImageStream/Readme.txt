Raditya Budianto
100836539


1. Copy the shell script 

2. To run countFPS.txt 
	type: /bin/bash countFPS.txt

# countFPS is used to count number of JPEG frame in buffer

3. to run imagestream.txt
	type: /bin/bash imagestream.txt

# imagestream is used to start video stream without needs to type lengthy commandline

Note: The file has extenstion .txt because I rewrote my script in notepad. There will be no problem for Linux to run the .txt file
Linux does not recognize file extension